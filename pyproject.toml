[project]
name = "portfolio-backend"
version = "0.1.0"
description = "FastAPI backend for the Priyanshu portfolio site"
requires-python = ">=3.11"
dependencies = [
  "fastapi[standard]>=0.115.0,<1.0.0",
  "uvicorn[standard]>=0.30.0,<1.0.0",
  "email-validator>=2.2.0,<3.0.0",
]

[tool.uv]
package = false
